# VideoTracker 手动测试用例

**版本**: v0.3.0
**编写日期**: 2026-05-11

---

## 一、P0 功能验收测试（对照 PRD AC-01 ~ AC-09）

### AC-01: 自动检测视频平台并提取信息

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-01-1 | 打开任意 B 站视频页面（如 `bilibili.com/video/BV1xx`） | 控制台输出 `[VideoTracker] 适配器已激活: B站` | P0 |
| AC-01-2 | 打开任意 YouTube 视频页面 | 控制台输出 `[VideoTracker] 适配器已激活: YouTube` | P0 |
| AC-01-3 | 打开任意爱奇艺视频页面 | 控制台输出 `[VideoTracker] 适配器已激活: 爱奇艺` | P0 |
| AC-01-4 | 打开任意腾讯视频页面 | 控制台输出 `[VideoTracker] 适配器已激活: 腾讯视频` | P0 |
| AC-01-5 | 打开非支持平台页面（如 `google.com`） | 控制台无 `[VideoTracker]` 输出 | P0 |

### AC-02: 阈值计时自动记录

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-02-1 | 设置阈值为 30 秒，打开 B 站视频并播放，等待 30 秒 | 记录自动保存，控制台输出保存日志 | P0 |
| AC-02-2 | 设置阈值为 30 秒，播放视频 10 秒后暂停 | 不触发记录 | P0 |
| AC-02-3 | 设置阈值为 0 秒，打开视频页面播放 | 立即触发记录 | P0 |
| AC-02-4 | 关闭自动记录开关，播放视频超过阈值 | 不触发自动记录 | P0 |
| AC-02-5 | 播放视频超过阈值后继续播放 | 仅记录一次，不重复记录 | P0 |

### AC-03: 手动快捷键记录

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-03-1 | 在视频页面按下 `Ctrl+Shift+V` | 当前视频立即记录，无论是否达到阈值 | P0 |
| AC-03-2 | 在非视频页面按下快捷键 | 无操作，不报错 | P0 |
| AC-03-3 | 连续快速按快捷键多次 | 记录正确更新（upsert），不产生重复记录 | P0 |

### AC-04: Popup 记录列表展示

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-04-1 | 点击插件图标打开 Popup | 显示已记录的视频列表，按最近观看时间排序 | P0 |
| AC-04-2 | 无记录时打开 Popup | 显示空状态提示 | P0 |
| AC-04-3 | 有多条记录时打开 Popup | 列表展示标题、集数、平台、进度百分比 | P0 |

### AC-05: 搜索与筛选

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-05-1 | 在搜索框输入视频标题关键字 | 列表实时过滤，只显示匹配项 | P0 |
| AC-05-2 | 选择平台筛选（如仅 B 站） | 列表只显示对应平台的记录 | P0 |
| AC-05-3 | 搜索无结果 | 显示空状态提示 | P0 |

### AC-06: 一键跳转继续观看

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-06-1 | 在 Popup 中点击某条记录 | 新标签页打开对应视频 URL | P0 |
| AC-06-2 | 点击 B 站多 P 视频记录 | 打开正确的分 P 页面（⚠️ 已知 BUG-1，当前可能回到 P1） | P0 |

### AC-07: 删除记录

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-07-1 | 在 Popup 中删除某条记录 | 记录从列表移除，存储中也删除 | P0 |
| AC-07-2 | 删除最后一条记录 | 列表显示空状态 | P0 |

### AC-08: Options 设置页面

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-08-1 | 点击 Popup 中的设置按钮 | 在新标签页打开 Options 设置页面（非 Chrome 扩展详情页） | P0 |
| AC-08-2 | 切换自动记录开关 | 设置立即保存，显示"✓ 设置已保存"提示 | P0 |
| AC-08-3 | 修改阈值下拉框 | 设置立即保存，阈值生效 | P0 |
| AC-08-4 | 刷新 Options 页面 | 设置保持修改后的值 | P0 |
| AC-08-5 | Options 页面三个 Tab 切换 | 记录管理 / 插件设置 / 站点管理 正常切换，内容正确 | P0 |
| AC-08-6 | 记录管理 Tab 中搜索视频 | 列表实时过滤 | P0 |
| AC-08-7 | 记录管理 Tab 中按平台筛选 | 列表只显示对应平台记录 | P0 |

### AC-09: 分页与批量操作

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-09-1 | 有 25+ 条记录时进入 Options「记录管理」Tab | 分页控件显示，每页 20 条，共 2 页 | P1 |
| AC-09-2 | 点击第 2 页 | 正确显示第 21~25 条记录 | P1 |
| AC-09-3 | 在搜索框中输入关键字 | 搜索结果重新分页，总数和页码更新 | P1 |
| AC-09-4 | 勾选多条记录 | 底部显示批量操作栏「已选 N 条」「删除选中」「取消选择」 | P1 |
| AC-09-5 | 勾选所有记录并点击「删除选中」 | 弹出确认对话框，确认后删除 | P1 |
| AC-09-6 | 点击全选 checkbox | 当前页所有记录被勾选 | P1 |
| AC-09-7 | 勾选记录时，批量操作栏出现 | 批量操作栏从上方滑入，带淡入和高度展开动画效果（0.3s ease-out），内容自适应动画容器 | P1 |
| AC-09-8 | 取消全部勾选时，批量操作栏消失 | 批量操作栏向上收起，带淡出动画效果（0.2s ease-in） | P1 |

### AC-09: SPA 路由监听

| 编号 | 测试步骤 | 预期结果 | 优先级 |
|------|---------|---------|--------|
| AC-09-1 | 在 B 站从视频 A 页内跳转到视频 B | 自动保存视频 A 记录，激活视频 B 适配器 | P0 |
| AC-09-2 | 在 YouTube 播放列表中切换视频 | 正确处理视频切换，分别记录 | P0 |

---

## 二、平台适配测试

### B 站（bilibili.com）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| BL-1 | 打开单 P 视频 `bilibili.com/video/BV1xx` | episode 为"正片" |
| BL-2 | 打开多 P 视频的第 2 P `?p=2` | episode 为"P2" |
| BL-3 | 播放视频后查看记录 | title、currentTime、duration 正确 |
| BL-4 | 检查 thumbnail | 提取 og:image meta 标签内容 |
| BL-5 | 跳转到续播 URL | URL 含 `?t=N` 参数，从上次位置播放 |
| BL-6 | 依次观看两个不同 B 站视频（`BV1xx` 和 `BV2yy`）| 两条记录分别独立保存，互不覆盖 |
| **BL-7** | **打开 Popup，点击 B 站视频记录** | **跳转到正确的视频页面（含正确 BV 号）** |

### YouTube（youtube.com）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| YT-1 | 打开普通视频 `youtube.com/watch?v=xxx` | 正确提取标题、时间 |
| YT-2 | 打开播放列表中的视频 | episode 为播放列表信息 |
| YT-3 | 检查 thumbnail | 使用 img.youtube.com 缩略图 |
| YT-4 | 跳转到续播 URL | URL 含 `?t=N` 参数 |
| YT-5 | 从短链 `youtu.be/xxx` 访问 | 正确检测和提取 |
| YT-6 | 依次观看 YouTube 视频 A（`?v=aaa`）和视频 B（`?v=bbb`）| 两条记录分别独立保存，互不覆盖 |
| **YT-7** | **打开 Popup，点击 YouTube 记录** | **跳转到正确的完整视频页面（含 `?v=xxx`）** |

### 爱奇艺（iqiyi.com）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| IQ-1 | 打开电视剧某集 | episode 为正确集数 |
| IQ-2 | 打开电影 | episode 为"正片" |
| IQ-3 | 播放并等待阈值 | 自动记录成功 |
| **IQ-4** | **打开 Popup，点击爱奇艺记录** | **跳转到正确的视频页面** |

### 腾讯视频（v.qq.com）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| QQ-1 | 打开电视剧某集 | episode 为正确集数 |
| QQ-2 | 打开电影 | episode 为"正片" |
| QQ-3 | 跳转到续播 URL | URL 含 `start=N` 参数 |
| **QQ-4** | **打开 Popup，点击腾讯视频记录** | **跳转到正确的视频页面** |

---

## 三、边界情况测试

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| EDGE-1 | 存储大量记录（100+ 条）后打开 Popup | 列表正常加载，滚动流畅 |
| EDGE-2 | 快速切换多个 B 站视频（< 2 秒间隔） | 不崩溃，正确记录最后停留的视频 |
| EDGE-3 | 视频 duration 为 0（直播流） | progress 为 0，不崩溃 |
| EDGE-4 | 视频 title 为空字符串 | extract 返回 null，不记录 |
| EDGE-5 | chrome.storage.local 达到存储上限 | 错误被捕获，不崩溃 |
| EDGE-6 | 页面刷新时触发 beforeunload | 记录正确保存 |
| EDGE-7 | 同时在 5+ 个标签页播放视频 | 各标签独立计时，互不干扰 |
| EDGE-8 | 插件更新后打开 Popup | 旧记录兼容展示 |
| EDGE-9 | 在 incognito 模式下使用 | 正常工作（取决于 manifest 配置） |
| EDGE-10 | 网络断开时使用 | 本地功能正常（无需网络） |
| EDGE-11 | 设置阈值后立即播放视频 | 新阈值立即生效 |
| EDGE-12 | URL 中含特殊字符（中文、#、&） | URL 规范化正确，不崩溃 |
| EDGE-13 | 视频播放中关闭浏览器 | beforeunload 尝试保存，下次启动可恢复 |
| EDGE-14 | YouTube 上依次观看 5 个不同视频 | 5 条记录各自独立保存，列表按时间倒序排列 |
| EDGE-15 | 混合平台记录：B站、YouTube、爱奇艺各看 1 个视频 | 3 条记录互不覆盖，Popoup 正确展示 |
| EDGE-16 | 手动保存和自动保存混合使用：YouTube 看视频 A（自动保存），切换视频 B（手动保存） | 两条记录独立，互不影响 |

---

## 四、自定义站点测试（v0.2.0 新增）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| CST-1 | 在 Options → 站点管理 Tab 中添加自定义站点 `example.com` | 站点列表显示 `example.com`，提示添加成功 |
| CST-2 | 打开 `example.com`（含 HTML5 `<video>` 元素） | 控制台输出 `[VideoTracker] 自定义站点适配器已激活: example.com` |
| CST-3 | 在自定义站点上播放视频超过阈值 | 记录自动保存 |
| CST-4 | 打开不含 `<video>` 元素的自定义站点页面 | Content Script 执行但 extract 返回 null，不发送心跳 |
| CST-5 | 在自定义站点上按 `Ctrl+Shift+V` | 手动保存当前视频 |
| CST-6 | 删除自定义站点后刷新页面 | 不再激活，控制台无 `[VideoTracker]` 输出 |
| CST-7 | 添加已存在内置站点列表中的域名 | 提示「该站点为内置站点，无需重复添加」 |
| CST-8 | 打开非内置、非自定义的普通页面（如 `google.com`） | 控制台输出 `[VideoTracker] 当前站点不在支持列表中，不激活` |

---

## 五、时间区间选择器测试（v0.3.0 改进）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| TF-1 | 在 Options 记录管理 Tab 中，默认不选择任何日期 | 所有记录正常显示，不限时间 |
| TF-2 | 点击「今天」快捷按钮 | 开始日期和结束日期自动填充为今天，只显示今天观看过的记录 |
| TF-3 | 点击「最近7天」快捷按钮 | 开始日期 = 7天前，结束日期 = 今天，显示最近7天内的记录 |
| TF-4 | 点击「最近30天」快捷按钮 | 开始日期 = 30天前，结束日期 = 今天，显示最近30天内的记录 |
| TF-5 | 点击「最近90天」快捷按钮 | 开始日期 = 90天前，结束日期 = 今天，显示最近90天内的记录 |
| TF-6 | 手动修改开始日期和结束日期输入框 | 按自定义日期区间筛选记录 |
| TF-7 | 点击快捷按钮后按钮高亮 | 胶囊样式按钮背景变蓝、文字变白 |
| TF-8 | 清空开始/结束日期输入框 | 恢复显示全部记录 |
| TF-9 | 时间筛选与搜索关键词组合使用 | 同时满足关键词和时间条件的记录才显示 |
| TF-10 | 时间筛选与平台筛选组合使用 | 同时满足平台和时间条件的记录才显示 |
| TF-11 | 切换时间筛选后，分页重置到第 1 页 | 页码变回 1，记录数更新 |
| TF-12 | 选择时间筛选后无可匹配记录 | 显示空状态「暂无记录」 |

## 六、重置按钮测试（v0.3.0 新增）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| RST-1 | 在搜索框输入关键字，选择平台，选择日期区间，然后点击「🔄 重置」| 搜索框清空、平台恢复「全部平台」、日期清空、页码回到第 1 页 |
| RST-2 | 重置后查看记录列表 | 全部记录正常显示 |

## 七、站点激活重试测试（v0.3.0 新增）

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| RET-1 | 打开含 WAF 验证的站点（如 cz4k.com），等待 WAF 验证通过后跳转到视频页 | Content Script 在 WAF 验证页初始化（未激活），验证通过 URL 变化后自动重试检测，最终成功激活适配器 |
| RET-2 | 打开视频页面但 video 元素延迟加载（如 SPA 应用） | 每 2 秒重试一次，最多 10 次，直到检测到 video 元素后激活 |
| RET-3 | 打开不含 video 元素的页面 | 重试 10 次（20 秒）后停止，控制台输出「重试检测已达最大次数」 |
| RET-4 | 在自定义站点（如 cz4k.com）上按 Ctrl+Shift+V 手动保存 | 手动保存正常触发，Toast 提示保存成功 |
| RET-5 | 在 B 站 SPA 页面内切换视频（从 video A 到 video B） | URL 变化后发送 PAGE_UNLOAD 保存 video A，重新检测激活 video B 适配器 |
| RET-6 | 打开通用适配器站点，video 元素在容器内（如 `<div class="player"><video>...</video></div>`）| 通用适配器 getVideoElement 通过方式 3（player 容器）成功找到 video |

## 八、测试环境要求

- Chrome 浏览器 120+
- 已加载 VideoTracker 插件（开发模式）
- 各平台测试账号（如需登录状态的视频）
- 开启浏览器控制台查看日志
