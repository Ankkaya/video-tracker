# Changelog

本文件记录 VideoTracker 的所有版本更新。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [0.0.7] - 2026-06-15

### ▶️ 恢复播放修复

- 点击 Popup 或 Options 中的记录时，会根据平台生成带播放进度参数的恢复 URL
- 页面加载后会读取 `t` / `start` 参数并主动将播放器定位到上次进度
- 成功定位后显示包含 “Resume” 的页面提示，满足 Edge 审核的可见反馈要求

## [0.0.6] - 2026-06-15

### 🛒 Edge 商店审核改进

- 修复 WXT 生成 manifest 时将 `options_ui.open_in_tab` 覆盖为 `false` 的问题，确保选项页在独立标签页中打开
- 空记录状态新增“添加示例记录”，审核员和新用户无需先观看视频即可验证记录列表、打开记录、删除记录等核心功能
- 新增 `docs/edge-certification-notes.md`，提供可粘贴到 Partner Center 的审核复现步骤

## [0.0.5] - 2026-06-15

### 🔒 加密云同步

- 新增加密同步密钥管理：首次开启同步时设置同步加密密码，之后使用本地 Data Key 解锁
- 观看记录和自定义站点会在本地加密后上传到 Supabase，云端只保存密文
- 支持加密数据的上传、下载和双向合并同步
- 退出登录时会清除本地同步解锁状态，避免下一个登录用户复用旧密钥

### 🧹 同步架构清理

- 移除旧版明文同步逻辑，不再读写 `records`、`custom_sites` 两张 Supabase 表
- Supabase schema 改为仅使用 `user_encryption_keys` 与 `encrypted_sync_blobs`
- 新增 `docs/supabase-drop-legacy-sync-tables.sql`，用于删除旧版明文同步表

### ✨ 体验优化

- 云端同步开关直接接入同步密码弹窗，首次设置需要二次确认密码
- 优化同步密码弹窗文案，提示同步密码的用途和保管要求
- 防止多次点击同步开关时重复弹出多个密码弹窗
- 修复点击遮罩关闭弹窗后同步开关一直处于加载状态的问题
- Popup 中点击“立即同步”成功后会自动关闭同步弹窗
- 自动同步默认关闭，开启后会在记录变更时触发加密同步

### 📦 发布

- 插件版本升级到 `0.0.5`
- 更新 README 和 Landing Page 的加密同步说明
- 提供 Chrome MV3 zip 发布包

## [0.0.2] - 2026-05-26

### 🌍 国际化支持

新增中英文双语支持，覆盖官网和浏览器插件。

### ✨ 新功能

#### 官网国际化
- 新增英文版官网页面 `landing/en/index.html`
- 添加中英文语言切换按钮
- SEO 优化：添加 `hreflang`、`lang` 属性和 Open Graph meta 标签

#### 插件国际化
- 集成 Vue I18n，支持动态语言切换
- 新增 `src/locales/` 翻译文件目录（zh-CN、en-US）
- 默认根据浏览器语言自动选择
- 在设置页面新增语言切换功能，选择会持久化保存

### 🐛 修复
- 修复 `SettingsTab` 中 `thresholdOptions` computed 属性的命名冲突，导致设置/站点 tab 切换报错

## [0.0.1] - 2026-05-11

### 🎉 首次发布

VideoTracker v0.0.1 正式发布！这是一款视频观看进度记录器，支持多平台视频自动记录与一键跳转。

### ✨ 功能

#### 核心功能
- **自动记录视频观看进度** — 访问支持的视频平台播放页面时，自动提取视频信息（标题、封面、播放进度等）并记录
- **阈值计时** — 默认观看满 30 秒才自动记录，避免短暂浏览产生无效记录；支持设置 0/10/30/60/120 秒阈值
- **手动快捷键记录** — 按 `Ctrl+Shift+V`（Mac: `Cmd+Shift+V`）立即记录当前视频，无需等待阈值
- **Popup 记录列表** — 点击插件图标查看所有记录的视频列表，按最近观看时间排序
- **一键跳转继续观看** — 点击记录直接打开对应视频页面，并自动定位到上次播放位置
- **搜索筛选** — Popup 内支持关键词搜索和按平台筛选，快速定位记录
- **删除记录** — 支持删除单条记录

#### 设置功能
- **自动记录开关** — 可在设置中开启/关闭自动记录功能
- **阈值设置** — 可自定义自动记录的观看时长阈值（0/10/30/60/120 秒）
- **快捷键说明** — 设置页面显示当前可用的快捷键

### 📺 支持平台
- B站（bilibili.com）
- YouTube（youtube.com）
- 爱奇艺（iqiyi.com）
- 腾讯视频（v.qq.com）

### 🔒 隐私
- 所有数据纯本地存储（`chrome.storage.local`），不上传任何服务器
- 不进行任何网络请求，不与外部服务通信
- 不收集、不传输、不分析任何用户数据

### 🛠 技术栈
- WXT — 浏览器扩展开发框架
- Vue 3 — 前端 UI 框架
- TypeScript — 类型安全的 JavaScript
- Chrome Storage API — 本地数据存储
